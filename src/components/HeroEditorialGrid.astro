---
import type { Article } from "../data/content";
import { CATEGORY_COLORS, CATEGORY_NAMES } from "../data/content";

interface Props {
  featured: Article;
  sideStories: Article[];
}

const { featured, sideStories } = Astro.props;
const catColor = CATEGORY_COLORS[featured.category];
---

<section class="max-w-[1400px] mx-auto px-4 md:px-8 pt-12 pb-0" style="background: #f0f0f0;">
  <div class="grid grid-cols-2 md:grid-cols-4 gap-3 auto-rows-fr" style="min-height: 500px;">
    <!-- Large -->
    <div class="col-span-2 row-span-2">
      <a
        href={`/blog/${featured.slug}`}
        class="group relative block overflow-hidden h-full"
        style="padding: 3.5%;"
      >
        <img
          src={featured.image}
          alt={featured.title}
          class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
          loading="lazy"
          decoding="async"
        />
        <span
          class="absolute top-6 left-6 text-[10px] tracking-[0.15em] uppercase font-semibold text-white z-10"
          style={`background-color: ${catColor}; padding: 0.25rem 0.6rem; border-radius: 2px; text-shadow: 0 1px 2px rgba(0,0,0,1);`}
        >
          {CATEGORY_NAMES[featured.category]}
        </span>
        <div class="absolute inset-0 flex flex-col justify-end z-10" style="padding: 2rem;">
          <h3 class="text-white leading-tight text-2xl md:text-3xl" style="text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
            {featured.title}
          </h3>
          <p class="text-sm mt-2 hidden md:block" style="text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000; color: #d0e8d0;">
          {featured.description}
          </p>
        </div>
        <img src="/images/ramka_2.png" alt="" class="absolute inset-0 w-full h-full pointer-events-none z-20" loading="lazy" />
      </a>
    </div>

    <!-- 4 small tiles -->
    {sideStories.map((story) => {
      const c = CATEGORY_COLORS[story.category];
      return (
        <div>
          <a
            href={`/blog/${story.slug}`}
            class="group relative block overflow-hidden h-full"
            style="padding: 3.5%;"
          >
            <img
              src={story.image}
              alt={story.title}
              class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"
              loading="lazy"
              decoding="async"
            />
            <span
              class="absolute top-5 left-5 text-[9px] tracking-[0.15em] uppercase font-semibold text-white z-10"
              style={`background-color: ${c}; padding: 0.2rem 0.5rem; border-radius: 2px;`}
            >
              {CATEGORY_NAMES[story.category]}
            </span>
            <div class="absolute inset-0 flex flex-col justify-end z-10" style="padding: 1.5rem;">
              <h4 class="text-white leading-tight text-sm md:text-base" style="text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;">
                {story.title}
              </h4>
            </div>
            <img src="/images/ramka_2.png" alt="" class="absolute inset-0 w-full h-full pointer-events-none z-20" loading="lazy" />
          </a>
        </div>
      );
    })}
  </div>
</section>
